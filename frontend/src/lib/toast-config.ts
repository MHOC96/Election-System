/** Default auto-dismiss for success/info toasts. */
export const TOAST_DURATION_MS = 4_000

/** Errors stay longer so users can read them (common UX pattern: ~5–6s). */
export const ERROR_TOAST_DURATION_MS = 6_000

/** Auto-dismiss for inline panels such as the member import summary. */
export const PANEL_AUTO_DISMISS_MS = 3_000
