# Backend API base URL (must include /api suffix in production)
VITE_API_URL=http://localhost:8000/api

# Optional — canonical site URL for SEO meta tags
# VITE_SITE_URL=https://your-frontend-domain.com
